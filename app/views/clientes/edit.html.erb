<div class="container">
  <h1>Editing Cliente</h1>

  <%= render 'form', cliente: @cliente %>

  <div class="btn-group">
      <%= link_to 'Show', @cliente, :class =>"btn btn-primary" %>
      <%= link_to 'Back', clientes_path, :class =>"btn btn-primary"%>
</div>
